/**
 *
 */
package testing;

/**
 * @author Sigi
 *
 */
public class AnimationTesting {

	/**
	 * @param args
	 */
	public static void main(final String[] args) {
		// JFrame window = new JFrame();
		// window.setBounds(500, 400, 500, 400);
		// window.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		// final JButton butt = new JButton();
		// window.add(butt);
		// window.setVisible(true);
		//
		// final String folder = "paladin";
		//
		// new Thread() {
		// public void run() {
		// while (true) {
		// try {
		// int delay = 200;
		// ButtonImages.addImageToButton(butt, new ImageIcon("images/" + folder
		// + "/idle1.jpg"));
		// sleep(delay);
		// ButtonImages.addImageToButton(butt, new ImageIcon("images/" + folder
		// + "/idle2.jpg"));
		// sleep(delay);
		// ButtonImages.addImageToButton(butt, new ImageIcon("images/" + folder
		// + "/idle3.jpg"));
		// sleep(delay);
		// ButtonImages.addImageToButton(butt, new ImageIcon("images/" + folder
		// + "/idle2.jpg"));
		// sleep(delay);
		// } catch (InterruptedException e) {
		// e.printStackTrace();
		// }
		// }
		// };
		// }.start();
	}
}
