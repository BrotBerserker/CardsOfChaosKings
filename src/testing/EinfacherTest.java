package testing;

/**
 * @author Roman Schmidt
 *
 */
public class EinfacherTest {

	/**
	 * @param args
	 */
	public static void main(final String[] args) {
		new EinfacherTest();
	}

	// public EinfacherTest() {
	// SchadenKarte blitz = new SchadenKarte("Blitz", 37, 18);
	// HeilKarte superheilung = new HeilKarte("Superheil", 20, 15);
	// ArrayList<Karte> karten = new ArrayList<Karte>();
	// karten.add(blitz);
	// karten.add(superheilung);
	// Spieler jones = new Spieler(karten);
	// Spieler stephen = new Spieler(karten);
	// for (int i = 0; i < 50; i++) {
	// jones.getHand()[i % 4].ausfuehren(jones, stephen);
	// stephen.getHand()[i % 4].ausfuehren(stephen, jones);
	// System.out.println(jones.getHand()[i % 4].getName());
	// System.out.println(stephen.getHand()[i % 4].getName());
	// System.out.println(jones.getHp());
	// System.out.println(stephen.getHp());
	// }
	// }

}
